version: 2.1

jobs:
  run_tests:
    docker:
      - image: circleci/node:10
    steps:
      - checkout
      - run: cd OTOT_Task_B_Api_Server
      - run:
          name: Install npm dependencies
          command: |
            npm install --save
      - run: mkdir ~/junit
      - run:
          command: npx mocha test --reporter mocha-junit-reporter
          environment:
            MOCHA_FILE: ~/junit/test-results.xml
          when: always
      - store_test_results:
          path: ~/junit
      - store_artifacts:
          path: ~/junit

workflows:
  build_test:
    jobs:
      - run_tests
